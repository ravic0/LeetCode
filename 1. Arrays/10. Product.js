const arr = [1,2,3,4,5];

const product = (arr) => {
    const answer = [];

    for(let i=0; i<arr.length; i++)
    {
        if(i==answer.length)
            continue;
        else 
    }

    return answer;
}